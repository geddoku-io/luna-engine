import { Settings } from '../types';

export const settingsDefault: Settings = {
    language: 'English',
    masterVolume: 100,
    musicVolume: 100,
    soundEffectsVolume: 100,
    voiceVolume: 100
};